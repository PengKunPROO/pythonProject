import numpy as np

np.random.seed(1)
X_assess = np.random.randn(5, 3)
Y_assess = np.random.randn(5, 3)
print(X_assess)
print(Y_assess)
np.random.seed(1)
X_assess = np.random.randn(5, 3)
Y_assess = np.random.randn(5, 3)
print(X_assess)
print(Y_assess)
